import { Color } from './types.js';
import DefaultConfigText from './config.default.yml';

/** The ID of the space tile. */
export const SpaceTileId = 'Space';

/** The size of a chunk in a map grid. */
export const ChunkSize = 16;

export const Black: Color = [0, 0, 0, 255];

export const GuidebookImageWidth = 128;

export const GuidebookImageHeight = 96;

export const DefaultConfig = DefaultConfigText;
